# Streamlit Community Cloud currently builds on Python 3.14, where older
# pandas/numpy releases have no prebuilt wheels. Lower bounds (not hard pins)
# let uv resolve the newest wheels that exist for the runtime it gives you.
# Verified against streamlit 1.61.1 / pandas 3.0.2 / numpy 2.4.4 / plotly 6.9.0
# and against streamlit 1.41.1 / pandas 2.2.3 / numpy 2.1.3 / plotly 5.24.1.
streamlit>=1.41,<2
pandas>=2.2
numpy>=1.26
plotly>=5.24
requests>=2.31

